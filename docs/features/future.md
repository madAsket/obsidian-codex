# Future

- Investigate running `codex status` in a GUI-safe pseudo-TTY (node-pty or `script`) to surface full CLI status (rate limits, session info) in the UI.
- Codex внутри плагина - как логинится? unified_exec ? https://github.com/openai/codex/blob/main/docs/config.md